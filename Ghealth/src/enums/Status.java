package enums;

/**
 * 
 * The Enum Query Status.
 * @author G5 lab group
 */
public enum Status {
	
	/** The exist case. */
	EXIST,
	
	/** The created case. */
	CREATED,
	
	/** The not exist case. */
	NOT_EXIST,
	
	/** The failed exception case. */
	FAILED_EXCEPTION,
	
	/** In session case. */
	IN_SESSION,
	
	
	/** The schedueld case. */
	SCHEDUELD, 
	/** The canceled case. */
	CANCELED, 
	/** The arrived case. */
	ARRIVED, 
	/** The noshow case. */
	NOSHOW,
	
	/** The cancel all case. */
	CANCEL_ALL,
	
	
	/** The is reg case. */
	/*Patient registration status*/
	IS_REG,
	
	/** The not reg case. */
	NOT_REG,
	
}
